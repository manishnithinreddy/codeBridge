package com.codebridge.session.model.enums; // Adapted package

/**
 * Enum for supported database types.
 */
public enum DbType {
    POSTGRESQL,
    MYSQL,
    ORACLE,
    SQLSERVER,
    MARIADB,
    MONGODB,
    OTHER
}
